# Gov Document Fetcher — ξεκίνημα

Εφαρμογή που κατεβάζει αυτόματα φορολογικά έγγραφα από τα portals της ΑΑΔΕ
(TaxisNet), με σωστή ονομασία αρχείων. Τρέχει τοπικά στον υπολογιστή σου.

| Τύπος πελάτη | Έγγραφα |
|---|---|
| Ατομική επιχείρηση | Ε1, Ε3, Εκκαθαριστικό, ΦΠΑ |
| Νομικό πρόσωπο (ΟΕ, ΕΕ, ΑΕ…) | Ν, Ε3, ΦΠΑ |

Δουλεύει σε **macOS** και **Windows**.

## Τι χρειάζεσαι

**Python 3.9 ή νεότερη** — τίποτα άλλο. Ο Chromium που χρειάζεται η
αυτοματοποίηση κατεβαίνει μόνος του την πρώτη φορά (~150 MB).

- **macOS**: υπάρχει ήδη. Έλεγξε με `python3 --version`
- **Windows**: κατέβασέ την από <https://www.python.org/downloads/> και στην
  εγκατάσταση τσέκαρε **«Add python.exe to PATH»**. Έλεγξε με `python --version`

## Βήματα

**1. Πάρε τον κώδικα**

```bash
git clone git@github.com:chatzilefterislef/gov-doc-fetcher.git
cd gov-doc-fetcher
```

Αν το `git clone` με SSH δεν δουλεύει, ζήτα από τον Λευτέρη πρόσβαση στο
repository — είναι ιδιωτικό.

**2. Ξεκίνα την εφαρμογή**

Μία εντολή τα κάνει όλα — περιβάλλον, πακέτα, Chromium, εκκίνηση:

```bash
python3 run.py
```

Στα **Windows** η ίδια εντολή είναι `python run.py`.

Η πρώτη φορά παίρνει ένα-δύο λεπτά όσο κατεβαίνει ο Chromium. Οι επόμενες είναι
άμεσες. Ο browser ανοίγει μόνος στο <http://localhost:8000>.

Αν η πόρτα είναι πιασμένη: `python3 run.py --port 8001`

**3. Δοκίμασε με έναν πελάτη**

Συμπλήρωσε κωδικούς TaxisNet, επωνυμία, χρονολογία, και ρύθμισε τον διακόπτη
**Ατομική Επιχείρηση** ανάλογα με τον πελάτη — αυτό καθορίζει ποια έγγραφα
ζητούνται. Πάτα **Έναρξη Λήψης**.

Θα ανοίξει ένα παράθυρο Chromium που πλοηγείται μόνο του· άφησέ το να δουλέψει
και μη κλείσεις το παράθυρο. Στο τέλος βγαίνει σύνοψη: τι κατέβηκε, τι δεν
υπήρχε, τι απέτυχε.

Τα PDF πάνε στο `~/Downloads/GovDocs` (Windows: `Downloads\GovDocs`).

## Δουλεύοντας με τον Claude σε αυτό το repo

Ζήτα «**ξεκίνα την εφαρμογή**» και θα χρησιμοποιήσει το έτοιμο preview config.

Το `CLAUDE.md` περιέχει τη δομή του κώδικα και — το σημαντικότερο — τις παγίδες
των portals της ΑΑΔΕ που έχουν κοστίσει χρόνο: οι τόνοι που χαλάνε τις ελληνικές
συγκρίσεις, τα λατινικά γράμματα που μοιάζουν με ελληνικά, τα «κουμπιά» που είναι
στην πραγματικότητα `<div>`, και ποιες ενέργειες δεν πατάμε ΠΟΤΕ. Διάβασέ το πριν
αλλάξεις κάτι στην αυτοματοποίηση.

## Όταν κάτι χαλάει

Τα portals της ΑΑΔΕ αλλάζουν, οπότε κάποιο βήμα μπορεί να χρειαστεί προσαρμογή.
Κάθε τρέξιμο γράφει αναλυτικό log με τα βήματα, τα URL και τα κουμπιά που βρήκε:

```bash
cat /tmp/gov_doc_fetcher.log
```

Δίπλα του υπάρχουν screenshots `gov_debug_*.png` από τα σημεία που απέτυχαν.
Δώσε αυτά τα δύο στον Claude και εντοπίζει γρήγορα το σημείο.

## Προσοχή

Είναι **ζωντανό φορολογικό portal**. Ο κώδικας έχει προστασίες που μπλοκάρουν
ενέργειες υποβολής, οριστικοποίησης, διαγραφής και πληρωμής (`NEVER_CLICK` στο
`automation/myaade.py`) — μη τις χαλαρώσεις.

Οι κωδικοί TaxisNet χρησιμοποιούνται μόνο για το login και **δεν αποθηκεύονται**
πουθενά, ούτε στο log. Τα κατεβασμένα PDF είναι στοιχεία πελατών· το
`.gitignore` αποκλείει τα `*.pdf` ώστε να μη ανέβουν ποτέ κατά λάθος.
